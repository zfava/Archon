apiVersion: apps/v1
kind: Deployment
metadata:
  name: archonai-gateway
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: archonai-gateway
    app.kubernetes.io/component: gateway
    app.kubernetes.io/part-of: archonai
spec:
  replicas: {{ .Values.gateway.replicaCount }}
  selector:
    matchLabels:
      app: archonai-gateway
  template:
    metadata:
      labels:
        app: archonai-gateway
        app.kubernetes.io/name: archonai-gateway
        app.kubernetes.io/component: gateway
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1654
        runAsGroup: 1654
        fsGroup: 1654
      containers:
        - name: gateway
          image: "{{ .Values.gateway.image.repository }}:{{ .Values.gateway.image.tag }}"
          imagePullPolicy: {{ .Values.gateway.image.pullPolicy }}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          ports:
            - containerPort: 8080
              name: http
          envFrom:
            - configMapRef:
                name: archonai-gateway-config
            - secretRef:
                name: archonai-secrets
          {{- with .Values.gateway.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.gateway.livenessProbe }}
          livenessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.gateway.readinessProbe }}
          readinessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: archonai-gateway-config
  namespace: {{ .Values.namespace }}
data:
{{- range $key, $value := .Values.gateway.env }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: archonai-gateway
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: archonai-gateway
    app.kubernetes.io/component: gateway
spec:
  type: {{ .Values.gateway.service.type }}
  selector:
    app: archonai-gateway
  ports:
    - port: {{ .Values.gateway.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
